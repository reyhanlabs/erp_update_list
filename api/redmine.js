import { REDMINE_BASE } from './redmine-config.js';

// NOTE: full file loaded from local - if this is wrong check next push
export default async function handler(req, res) {
  res.status(500).json({ error: 'placeholder - will replace' });
}
